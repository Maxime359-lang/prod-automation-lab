[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prod-automation-lab"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["flask==3.0.0"]

[tool.setuptools]
packages = ["app"]
